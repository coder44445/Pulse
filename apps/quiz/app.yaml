# apps/quiz/app.yaml
name: quiz

stacks:
  backend:
    compose: ../open-quiz-svc/docker-compose.yml

dependencies:
  - persona
  - core-infra

idle_timeout: 10m

health:
  backend: http://localhost:8000/healthz

routes:
  - /api/quiz